---
"@discordkit/native": minor
---

Added @discordkit/native: a functional TypeScript bridge to the Discord Social SDK for native desktop runtimes (Electron, Tauri, headless Node). v0 ships rich presence + the client lifecycle (ambient singleton, status signal, using-disposable cleanup) over a Koffi FFI backend behind a node:ffi-ready seam.
