---
---

Build the Gateway Event Inspector UI on the `with-cloudflare` example: React SPA served by the Worker, streaming live Gateway traffic from the Durable Object over a WebSocket. Example-only — no published package changes.
