# Varlock-generated env types (regenerated by the `typegen` task)
env.d.ts

# Build output
dist/
# Bundled preload (generated by the `preload` pack task from electron/preload.ts)
electron/preload.bundle.cjs

# Playwright
test-results/
playwright-report/

# Local env (never commit real values)
.env
.env.local
