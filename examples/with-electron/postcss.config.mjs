// Tailwind CSS v4 — the single PostCSS plugin handles everything (including
// vendor prefixing), so no `tailwind.config` or `autoprefixer` is needed.
const config = {
  plugins: {
    "@tailwindcss/postcss": {}
  }
};

export default config;
