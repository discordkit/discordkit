/** Renderer typing for the voice namespace. */
export type { VoiceBridge, CallSnapshot } from "../channels/voice.js";
export type {
  AudioDevice,
  CallStatus,
  AudioMode,
  VADThreshold,
  VoiceState
} from "@discordkit/native/voice";
